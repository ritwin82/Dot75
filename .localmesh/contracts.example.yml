version: 1
mappings:
  - template: users
    enabled: true
    bindings:
      table: public.users
      id: id
      identity: email
      created_at: created_at
  - template: orders
    enabled: true
    bindings:
      table: public.orders
      id: id
      customer_id: user_id
      status: status
