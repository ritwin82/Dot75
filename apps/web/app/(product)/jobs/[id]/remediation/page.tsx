import {RemediationLab} from "../../../../components/remediation-lab";
export default async function RemediationPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="remediation-page"><section className="compatibility-heading"><a className="product-link" href={`/jobs/${encodeURIComponent(id)}`}>Back to investigation</a><div className="product-kicker">Verified local AI</div><h1>Remediation lab</h1><p>Generate SQL candidates locally and accept only those that pass the original deterministic replay. Nothing here can commit, push, create a repair PR, or change the GitHub verdict.</p></section><RemediationLab jobId={id}/></div>;}
