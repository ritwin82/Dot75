export const dynamic="force-dynamic";
async function getJob(id:string){const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:4100"}/api/jobs/${id}`,{cache:"no-store"});return response.ok?response.json():null;}
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;const job=await getJob(id);if(!job)return <section className="hero"><h1>Job not found.</h1></section>;const r=job.result??{},orders=r.orders??[],findings=[...orders.flatMap((o:any)=>o.findings??[]),...(r.contracts??[]),...(r.rollbacks??[]).flatMap((x:any)=>x.findings??[]),...(r.performance??[])];return <><section className="hero"><div><div className="eyebrow">{job.owner}/{job.repo} · PR #{job.pr_number}</div><h1>{job.status}</h1><p>{r.explanation?.cause??job.error??"Validation is still in progress."}</p></div><a className="pill" href="/">← All checks</a></section><div className="detail"><div className="stack"><div className="card"><div className="eyebrow">Execution orders</div>{orders.map((o:any,i:number)=><div className="row" key={i}><b>{o.order.map((n:number)=>`#${n}`).join(" → ")}</b><span className={`status ${o.passed?"passed":"failed"}`}>{o.passed?"passed":"failed"}</span><span className="sub">{o.durationMs} ms</span></div>)}</div><div className="card"><div className="eyebrow">Findings</div>{findings.length?findings.map((f:any,i:number)=><div className={`finding ${f.severity}`} key={i}><b>{f.title}</b><div className="sub">{f.message}</div></div>):<div className="empty">No deterministic findings.</div>}</div></div><div className="stack"><div className="card"><div className="eyebrow">Affected objects</div><div className="objects">{(r.affectedObjects??[]).map((o:any)=><span className="object" key={o.id}>{o.id}</span>)}</div></div><div className="card"><div className="eyebrow">Repair guidance</div><p>{r.explanation?.forwardFix??"Pending"}</p><div className="sub">Source: {r.explanation?.source??"—"}</div></div></div></div></>}
