{
  "name": "@localmesh/action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@localmesh/github": "workspace:*",
    "@localmesh/shared": "workspace:*",
    "@octokit/rest": "^22.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@localmesh/engine": "workspace:*"
  }
}
